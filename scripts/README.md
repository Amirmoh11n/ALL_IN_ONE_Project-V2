# scripts

Convenience shell scripts for common project operations (e.g. training, evaluation, export entry points).

- `run.bash` — placeholder entry point.

Status: structural placeholder; content pending implementation approval.
