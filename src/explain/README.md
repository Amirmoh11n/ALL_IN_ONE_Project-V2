# src/explain

Offline explainability (artifacts only for V2.0).

- `gradcam.py` — Grad-CAM / Grad-CAM++ heatmaps for a few samples per class.
  Output: `artifacts/explainability/`. Not wired into the web app.
