# docker

Containerization files for deploying the backend + inference pipeline (e.g. to AWS).

- `Dockerfile` — builds the backend/inference image.
- `docker-compose.yml` — orchestrates services for local/deployment runs.

Status: structural placeholders; content pending implementation approval.
