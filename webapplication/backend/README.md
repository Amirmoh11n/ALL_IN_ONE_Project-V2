# webapplication/backend

FastAPI backend. Exposes an endpoint that accepts an uploaded MRI image and returns the predicted class by
calling the inference pipeline in `src/inference`.

Status: structural placeholder; endpoints pending implementation approval.
