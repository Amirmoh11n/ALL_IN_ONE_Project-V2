# configs

Holds `config.yaml`, the single source of truth for experiment settings (data paths, model, training
hyperparameters, evaluation, export targets, MLflow tracking, logging level).

Goal: enable running different experiments without touching source code.
